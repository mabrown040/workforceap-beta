# Audit beat `run_20260823_0215`

Map cut: blast radius (11 clusters, 161 files) after adding scale seeds
(`ent_cluster_prisma_db`, `ent_cluster_request_cost`, `ent_cluster_rate_limit`,
`ent_cluster_payments`, `ent_cluster_jobs_cron`, `ent_cluster_portal_hot`).
Hottest cluster by git heat: `ent_cluster_request_cost` (255 hotspot commits).

Four auditors, isolated contexts. Rank: `AUDIT_RUN_ID=run_20260823_0215 node scripts/audit-rank.mjs`
(17 unique open, 0 duplicates collapsed).

Prior beat `run_20260816_0345` claims were re-verified against master after
PR #2187 / enroll+consent work. Status transitions (evidence unchanged; status
only): 11 → `verified`, 1 → `wontfix` (`clm_hot_stamp_update`). Those files
were not rewritten for ranking games — they left the open set so this beat’s
scale claims could rank.

**Human gate: which of the top slice ships. Nothing below was patched.**

## Top slice (ranked, code)

| # | id | severity | auditor | title |
|---|---|---|---|---|
| 1 | `clm_hot_dashboard_query_fanout` | prod-break | hot-paths | Member dashboard home issues 20+ Prisma reads plus Coursera sync |
| 2 | `clm_hot_layout_auth_every_request` | prod-break | hot-paths | Root layout and middleware hit Supabase + Prisma on every HTML request |
| 3 | `clm_secrets_admin_pages_unscoped` | prod-break | secrets | Admin layout is global `isAdmin()`; many admin pages query all tenants |
| 4 | `clm_secrets_orphan_user_default_org` | prod-break | secrets | `ensureAppUserProvisioned` always writes orphans into default org |
| 5 | `clm_hot_admin_unbounded_scans` | ship-break | hot-paths | Admin training-progress loads up to 20k `courseProgress` rows unscoped |

Anchors: `graph/evidence/<claimId>.txt` and `graph/evidence/run_20260823_0215_verifies.txt`.

## Ground check (orchestrator, not a fifth auditor)

- **#1 and #2 are one blast (request-path cost).** Layout already paid
  `getUser` / org / branding / i18n before `dashboard/page.tsx` starts its 24
  Prisma calls. The 2026-06-30 pooler incident and the GUC-504 comment in
  `lib/db/prisma.ts:88-94` are the same failure mode. Fixing only the
  dashboard without cutting layout auth still leaves every public page
  expensive.
- **#3 and #5 are one blast (admin tenant + volume).** Partner pages were
  scoped after the last beat (`withTenantScope` on `app/admin/partners/[id]/page.tsx`).
  Training-progress / placements / students / users were not. The 20k-row
  take is the scale symptom of the same missing org filter.
- **#4 is the multi-tenant landmine.** Harmless while every user is
  `workforceap`. Wrong the first time a custom-domain org user is missing a
  `users` row. Do not “also clean up” `ensureAppUser` while touching layout
  cost — different cut.
- **`clm_hot_stamp_update` remains `wontfix`** (prior ground check: null-guarded
  `updateMany` is the fix, pinned by `route.test.ts`).

## Below slice (not acted on)

- `clm_hot_apply_ratelimit_failopen` — apply/signup/AI/voice fail-open without Redis
- `clm_hot_i18n_full_catalog_every_request` — 184KB `messages/en.json` every request
- `clm_hot_custom_domain_branding_uncached` — custom-domain branding skips `unstable_cache`
- `clm_secrets_rls_guc_fail_open` — GUC off; table owner bypasses RLS
- `clm_secrets_qa_bypass_default_secret` — committed QA bypass default
- `clm_dead_duplicate_nudge_crons` — two weekly nudge jobs
- `clm_dead_consent_form_unmounted` — `ParentalConsentForm` still unused
- `clm_dead_seeds_unwired_still` — CHS scripts not in `db:seed`
- `clm_deps_prisma5_use_middleware` — `$use` blocks Prisma 6
- `clm_deps_marketing_nested_install` — Vercel `marketing && npm ci` on every prod build
- hygiene: Astro enroll shadow, `@heroicons/react` prod dep

## Patched / verified

**This beat:** no product code patched. Fixer did not open. Verify commands
were run as *evidence that the claims are real*, not as post-fix greens.

**Prior beat claims now `verified` on master (re-check, not this fixer’s work):**

- Enroll Next route + `enrollmentPageEnabled` reader (`lib/enroll/resolveEnrollmentPartner.ts`)
- Admin school write fields + `lib/partner/adminSchoolPartner.ts`
- Consent gate (`lib/admin/courseraConsentGate.ts` used by approval)
- Partner admin pages tenant-scoped
- `wap_partner_ref` clear now includes `httpOnly` / `sameSite` / `secure`
- CHS script writes catalog

## Proposed rules (not accepted)

New `proposed` rows in `graph/rules.json`:

- `rul_layout_skip_getuser_when_anonymous`
- `rul_provision_uses_request_org`
- `rul_admin_pages_tenant_scoped`
- `rul_no_rls_guc_without_force`
- `rul_dashboard_query_budget`
- `rul_apply_ratelimit_fail_closed_prod`

Prior proposed rules whose checks now pass on master (human should **accept**
or retire): `rul_enroll_flag_must_serve`, `rul_admin_school_is_data_entry`,
`rul_partner_ref_clear_matches_plant`, `rul_admin_partner_pages_tenant_scoped`.

## What to tell the human (20 minutes)

1. Do **not** enable `WAP_RLS_GUC_ENABLED` as a scale fix. It is the opposite.
2. Ship request-path cost as one cut: skip `getUser()` on anonymous layout +
   skip `getSession()` on public middleware paths that do not need refresh.
   That is claim #2. Smallest safe slice if you want a first PR.
3. Ship dashboard query budget as a second cut (claim #1). Do not add more
   `prisma.*` to `dashboard/page.tsx`.
4. Ship admin tenant scope + pagination together (claims #3 + #5) before a
   second org goes live.
5. Claim #4 only matters when a second org exists — schedule it with
   custom-domain launch, not with marketing perf.
6. Full sequenced plan: `docs/SCALE-FIX-PLAN.md`.
