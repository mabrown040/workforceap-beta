import PortalRouteLoading from '@/components/portal/PortalRouteLoading';

// Career Brief renders a metric-card strip + list/summary blocks — closer
// to the "dashboard" skeleton shape than "detail"'s paragraph lines.
export default function Loading() {
  return <PortalRouteLoading variant="dashboard" />;
}

