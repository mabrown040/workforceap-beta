# Archived personal-workspace patches

These files preserve historical variants and are excluded from the patch runner's
non-recursive active inventory. Active nonprofit targets are explicitly listed in
`lib/elevenlabs/agentRegistry.ts`; the April migration evidence remains unchanged
under `scripts/elevenlabs/state/`. Do not use an archived ID as a runtime fallback.
