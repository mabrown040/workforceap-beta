# Audit beat `run_20260825_1630`

Map cut: blast radius (`node scripts/audit-map.mjs` → 5 clusters / 101 files). Focus: apply / eligibility / employer / invite email serverless freeze class (sibling of apply signup `after()` fix `ba1160f8` / `d165391b`).

Four auditors, isolated claim files. Rank: `scripts/audit-rank.mjs`. Prior partner-school open claims from `run_20260816_0345` archived under `graph/archive/claims/` so this beat’s rank reflects the email cluster (human gate: apply-email ship-selected; partner-school remains deferred).

## Top slice at start of fix (ship-break)

| # | id | severity | title | outcome |
|---|---|---|---|---|
| 1 | `clm_hot_elig_member_no_after` | ship-break | Member eligibility fire-and-forget | **verified** — `after()` |
| 2 | `clm_hot_elig_token_no_after` | ship-break | Token `/api/q/[token]/submit` fire-and-forget | **verified** — `after()` |
| 3 | `clm_hot_employer_approve_reject_no_after` | ship-break | Employer approve/reject emails | **verified** — `after()` |
| 4 | `clm_hot_employer_signup_no_after` | ship-break | Employer welcome + admin alert | **verified** — `after()` |
| 5 | `clm_hot_prescreen_no_after` | ship-break | Pre-screening admin alert | **verified** — `after()` |

Also patched same-class latent in residual slice: `clm_hot_invite_accept_no_after` → **verified**.

## Already OK (no code change)

- Apply signup confirmation + admin alert — already `after()` (`ba1160f8` / `d165391b`); confirmation copy distinguishes auto receipt vs advisor 1–2 day follow-up.
- Contact form — `await resend.emails.send` before JSON.
- Admin eligibility campaign / token-link send — `await sendEligibilityLink`.
- Member program enroll — already `after()` for course enrolled + kickoff.
- Crons — await Resend inside long-lived cron invocations.

## Remaining top slice (not patched — ops / product)

| id | severity | why left |
|---|---|---|
| `clm_dead_confirmation_email_orphan` | latent | `/api/apply/confirmation-email` has no client callers; rate-limited + application-anchored. Delete or keep as manual resend — human call. |
| `clm_secrets_resend_silent_miss` | latent | Missing `RESEND_API_KEY` soft-fails with warn. Ops must keep key set in Vercel; not a code bug. |

## Patched files

- `app/api/member/eligibility/route.ts`
- `app/api/q/[token]/submit/route.ts`
- `app/api/member/pre-screening/route.ts`
- `app/api/employer/signup/route.ts`
- `app/api/admin/employers/[id]/approve/route.ts`
- `app/api/admin/employers/[id]/reject/route.ts`
- `app/api/invite/accept/route.ts`
- `lib/email/serverlessAfterGuard.test.ts` (regression guard)
- `docs/EMAIL_SETUP.md`

## Verify

```text
node --import tsx --test lib/email/serverlessAfterGuard.test.ts emails/eligibility-screening-ws5.test.ts app/api/employer/signup/route.test.ts
→ 13 pass / 0 fail
```

Evidence: `graph/evidence/clm_hot_email_after_verify.txt` + per-claim `rg` dumps.

## Proposed rule

`rul_serverless_email_after` (proposed): API Resend sends after success must use `after()` or await; guard test above.

## Human / ops residual risks

1. **Resend key** must be present in production; silent miss remains latent.
2. **Spam folders** — copy already tells applicants to check spam.
3. **Orphan confirmation-email route** — decide keep vs delete.
4. **Partner-school beat** (`run_20260816_0345`) still deferred in `graph/archive/claims/`.
5. Do **not** enable `WAP_RLS_GUC_ENABLED` as part of this beat.

## Ship SHAs + deploy

| ref | tip SHA |
|---|---|
| branch `cursor/apply-email-blast-audit-942e` | `db3018ff` |
| `master` (cherry-picked) | `6096ae88` |
| prior apply signup `after()` | `ba1160f8` / `d165391b` |

Vercel production `dpl_252UsLYhvv9Ws3YfbTey1vZCqYfJ` for `6096ae88` → **READY** (aliases include workforceap.org / www.workforceap.org).
