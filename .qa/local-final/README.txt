Requested final local screenshots could not be captured in this sandbox.

Reason:
- Local HTTP servers cannot bind to a port here (`listen EPERM` on `next dev`).
- Without a running local server, Playwright/browser capture of the updated routes is not possible from this environment.

Requested routes:
- /
- /what-we-do
- /for-employers
- /how-it-works
- /programs
- /find-your-path
- /salary-guide
- /jobs
- /blog
- /faq
- /contact
- /apply

Code changes for the Stitch overhaul were completed in-repo.
