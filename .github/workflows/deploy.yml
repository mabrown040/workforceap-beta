# DISABLED — self-hosted runner / PM2 deploy replaced by Vercel.
# Production deploys now happen automatically via Vercel on push to master.
# This file is kept for reference only. Do not re-enable without removing the
# self-hosted runner dependency.
#
# Original flow: push to master → self-hosted runner on prod box → npm ci → next build → pm2 restart
# New flow: push to master → Vercel production deploy (automatic via Vercel GitHub integration)
#
# To re-enable SSH-based prod deploy in the future, see docs/ci-cd.md

name: Deploy to Production (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type DEPLOY to confirm manual trigger"
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. Production deploys via Vercel automatically."
