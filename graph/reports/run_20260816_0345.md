# Audit beat `run_20260816_0345`

Map cut: blast radius (5 clusters, 75 files). Four auditors, isolated contexts. Rank: `scripts/audit-rank.mjs` (13 unique, 0 duplicates collapsed).

**Human gate: which of the top slice ships. Nothing below was patched.**

## Top slice (ranked, code)

| # | id | severity | auditor | title |
|---|---|---|---|---|
| 1 | `clm_dead_enroll_flag` | prod-break | dead-code | `enrollmentPageEnabled` + `PartnerProgramCatalog` are write-only |
| 2 | `clm_hot_consent_ungated` | prod-break | hot-paths | Guardian consent never mounts; does not gate Coursera |
| 3 | `clm_hot_stamp_update` | prod-break | hot-paths | Signup `updateMany` stamps `sponsoredByPartnerId` |
| 4 | `clm_secrets_admin_partners_unscoped_pii` | prod-break | secrets | Admin partner pages load emails/salaries with no tenant scope |
| 5 | `clm_dead_admin_unpublish` | ship-break | dead-code | Admin create/PATCH cannot set school/sponsor/catalog fields |
| 6 | `clm_dead_enroll_route` | ship-break | dead-code | `/enroll/<slug>` has no Next route; only Astro CHS page |
| 7 | `clm_hot_school2_no_page` | ship-break | hot-paths | Admin cannot stand up school #2 |

## Ground check (orchestrator, not a fifth auditor)

- **`clm_hot_stamp_update` — recommend `wontfix`.** The `updateMany` is the *fix* for unguarded upsert-update. It only writes when `fundingSource` and `fundingNotes` are both null (`app/api/apply/signup/route.ts` ~443–456). Tests in `route.test.ts` pin create-only / null-guarded semantics. Ranker cannot know that; title looked like a prod-break.
- **`clm_dead_enroll_route` + `clm_hot_school2_no_page` + `clm_dead_admin_unpublish` + `clm_dead_enroll_flag` are one blast.** Master has Phase A + B1 + B2 (Astro `/enroll/concordia`, schema, cookie + signup stamp) and not the dynamic page / admin school write path. Printed student link works on Vercel (marketing `dist` copied into `public/`). Self-host `npm run build` and any slug other than `concordia` 404. School #2 is not admin data entry on this branch.
- **`clm_hot_consent_ungated` + `clm_secrets_consent_gate_missing` + `clm_dead_consent_orphan` are one blast.** `ParentalConsentForm` has no importers. `lib/admin/courseraConsentGate.ts` does not exist. Concordia Astro copy still promises school-collected consent before activation.
- **`clm_secrets_admin_partners_unscoped_pii` is real vs `isAdminInOrg`.** Pages under `app/admin/partners` use `prisma.partner.findUnique` / `findMany` with no `withTenantScope`. Layout gates on global `isAdmin()`. API routes under `/api/admin/partners` were scoped. Single-tenant today; custom-domain tenant is the break. Confirm before treating as ship-now.
- **`clm_secrets_ref_cookie_clear_attrs` (below slice, latent).** Plant in `middleware.ts` uses `httpOnly` + `sameSite: 'lax'` + `secure` in prod. Clear in signup is `{ maxAge: 0, path: '/' }` only. Shared-device mis-attribution if the browser keeps the original cookie.

## Below slice (not acted on)

- `clm_secrets_consent_gate_missing` (latent) — same blast as #2
- `clm_dead_consent_orphan` (latent) — same blast as #2
- `clm_dead_seeds_unwired` (latent) — `db:seed` does not run `create-chs-partner.ts`
- `clm_dead_chs_catalog_gap` (latent) — prod CHS script writes no catalog
- `clm_hot_empty_catalog` (latent) — publish + 0 catalog rows
- `clm_secrets_ref_cookie_clear_attrs` (latent) — cookie clear attrs
- deps auditor: empty array (no grounded dep break in the subgraph)

## Patched / verified

None. Fixer did not open. Verify did not run. Red does not go back.

## Proposed rules (not accepted)

See `graph/rules.json` status `proposed` from this run. Human accepts or drops.

## Existing work that would close several claims

PR #2187 (`cursor/partner-school-enrollment-scale-1484`) drafts the dynamic `/enroll/[school]` page, admin school fields, and consent gate. Merging that PR is a candidate for the human “which fixes ship” step — it is not done here.

## What to tell the human (20 minutes)

1. Drop `clm_hot_stamp_update` (`wontfix`) unless you want the null-guard rewritten.
2. Ship school #2 as one cut: Next `/enroll/[slug]` + admin sponsor/catalog fields + catalog reader (`enrollmentPageEnabled` must actually serve). That is claims 1, 5, 6, 7.
3. Ship consent as a second cut: mount + `courseraApprovalBlockedByConsent` before B4B activate. Claim 2 (+ the two latent twins).
4. Decide tenant-scope on admin partner pages (claim 4) and cookie-clear attrs (below slice) separately.
