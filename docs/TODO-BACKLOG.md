# TODO / FIXME Backlog

> Auto-generated by TODO cleanup sweep on 2026-05-13.
> Remaining items after removing already-fixed comments and applying quick wins.

---

## Quick Wins Applied (✅)

| File | Line | Action |
|------|------|--------|
| `app/admin/board/print/page.tsx` | 83 | Extracted `<PrintButton />` client component (`components/admin/PrintButton.tsx`) to replace `javascript:window.print()` anchor hack. |

---

## Real Backlog

### 1. Counselor student list filter chips
- **File:** `app/(portal)/counselor/students/page.tsx:172`
- **Comment:** `{/* TODO: Add real filter chips (At Risk, Upcoming Session) when student list is converted to a client component with filter state. Removed non-functional decorative chips that looked clickable. */}`
- **Category:** feature
- **Priority:** P1
- **Effort:** small
- **Notes:** Requires converting the student list page to a client component (or adding a client island) to manage filter state. The old decorative chips were removed because they looked clickable but did nothing.

### 2. Remove legacy PlacedOutcome sync
- **File:** `app/api/admin/members/[id]/placed-outcome/route.ts:107`
- **Comment:** `// TODO: Remove once PlacedOutcome is fully retired.`
- **Category:** refactor
- **Priority:** P2
- **Effort:** small
- **Notes:** PlacedOutcome is still referenced in `prisma/schema.prisma`, `app/admin/members/[id]/page.tsx`, and `components/admin/AdminMemberPlacedOutcomeForm.tsx`. Cannot remove until all consumers and the DB table are migrated.

### 3. Wire SLO endpoint to real monitoring APIs
- **File:** `app/api/health/slo/route.ts` (lines 78, 98, 118, 139, 160, 181)
- **Comment:** `* TODO: wire to real Sentry / Vercel APIs in Sprint D.2.`
- **Category:** feature
- **Priority:** P1
- **Effort:** medium
- **Notes:** Six stubbed measurement helpers (`measureUptime`, `measureDashboardLatency`, `measureOutcomesLatency`, `measureEmailDelivery`, `measureCrossTenantIsolation`, `measureCourseraIngestion`) all return `current: null` and `status: 'unknown'`. Sprint D.2 was the planned milestone. The route is admin-only and has no in-repo consumers yet. Either wire the APIs or delete the dead code per the `@deprecated` JSDoc note.

### 4. Query memberEvent for interview practice completion
- **File:** `lib/member/getMemberState.ts:289`, `lib/member/getMemberState.ts:325`
- **Comment:** `hasCompletedInterviewPractice: false, // TODO: query memberEvent`
- **Category:** bug
- **Priority:** P1
- **Effort:** small
- **Notes:** The dashboard (`app/(portal)/dashboard/page.tsx`) already queries `prisma.memberEvent.findFirst({ where: { userId, eventName: 'career_os.interview_practice_completed' } })`. `getMemberState` hardcodes `false` in two places, which means `nextBestActions` and downstream consumers may show incorrect state. Port the dashboard query into `getMemberState`.

### 5. Coursera launch route regression tests
- **File:** `lib/coursera/launchRoute.test.ts:4`
- **Comment:** `* TODO: Route-level regression test for GET /api/member/coursera/launch`
- **Category:** feature (testing)
- **Priority:** P1
- **Effort:** medium
- **Notes:** Blocked on test infra supporting Next.js route mocking with `node:test`. The file contains a detailed scaffold of test cases to cover when migration to vitest or a `next-test-helpers` layer happens. E2E smoke coverage exists in `tests/e2e/sprint-p2-xapi-coursera-smoke.spec.ts` for the unauthenticated path only.

### 6. Persist Coursera total learning hours
- **File:** `lib/coursera/syncUserFromB4B.ts:450`
- **Comment:** `// TODO(persist-learning-hrs): once we add CourseProgress.totalLearningHours (Float?) we'll persist gradebookRow.approxTotalLearningHrs here.`
- **Category:** feature
- **Priority:** P1
- **Effort:** small
- **Notes:** Requires a Prisma migration to add `totalLearningHours` (or similar) to `CourseProgress`. The value is already available from `gradebookRow.approxTotalLearningHrs` but is not persisted. Held back from the original PR to avoid scope creep.

---

## Stats

| Metric | Count |
|--------|-------|
| Comments found (total grep hits) | 45 |
| False positives (TODO_courseId placeholders, error messages, explanatory text) | 32 |
| Real TODO comments | 13 |
| Already fixed / removed | 1 (PrintButton extraction) |
| Quick wins applied | 1 |
| Items in backlog | 6 |
